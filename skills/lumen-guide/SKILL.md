---
name: lumen-guide
description: Generate a multi-tab HTML document (shell + CSS + JS + tab fragments) for user guides, architecture overviews, comparison matrices, recaps, and roadmaps. Use when the user asks for a "guide", "doc", "architecture overview", "recap", or content with multiple sections that should be tabbed.
version: 0.1.0
---

# lumen-guide

> Status: scaffold. Implementation lands in v0.2.

## Sources to copy from (do not re-create)

- `roxabi-forge` (forge-guide) — split-shell architecture (shell HTML + per-artifact CSS/JS + tab fragments), CSS component library (`.hero` variants, `.section-label`, `.card.accent/info/warning/critical`, `.stat-grid`, `.steps`, `.phases`, `.kv-strip`, `.summary-card`, `.finding`, `details.disclosure`), 3-layer information architecture (glance/scan/deep), Frame → Structure → Style → Deliver methodology
