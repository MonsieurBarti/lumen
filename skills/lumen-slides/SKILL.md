---
name: lumen-slides
description: Generate a magazine-quality scroll-snap presentation deck as a single HTML file. 10 slide types (title, section, content, quote, image, code, comparison, table, diagram, closing) with reveal animations and keyboard + touch navigation. Use when the user asks for a "deck", "slides", "pitch", or "presentation".
version: 0.1.0
---

# lumen-slides

> Status: scaffold. Implementation lands in v0.2.

## Sources to copy from (do not re-create)

- `roxabi-forge` (forge-slides) — SlideEngine (scroll-snap nav, keyboard ↑/↓/PgUp/PgDn/Home/End, touch swipe, prefers-reduced-motion), 10 slide patterns with exact `.slide--{type}` selectors, `.reveal` stagger animations, 6 aesthetic presets
- `visual-explainer` (generate-slides) — single-file HTML output, design-system palette + typography
