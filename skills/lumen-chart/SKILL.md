---
name: lumen-chart
description: Generate a single-file SVG data chart (bar, pie, line, area, scatter, radar, funnel, bubble). Use when the user provides numeric data and asks for a "chart", "graph", "plot", "trend", "distribution", or "comparison".
version: 0.1.0
---

# lumen-chart

> Status: scaffold. Implementation lands in v0.2.

## Sources to copy from (do not re-create)

- `gmdiagram` (gm-data-chart plugin) — 9 chart types, 12 visual styles, Nice Numbers algorithm for axis ticks, JSON schema → SVG pipeline
- `roxabi-forge` — aesthetic system, hero + stat-grid composition, 3-layer information architecture (glance/scan/deep)
